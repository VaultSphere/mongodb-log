# MongoDB Log Analyzer 项目规范

## 项目目标

本项目是供运维人员在个人电脑上离线使用的 MongoDB 日志与 FTDC 指标分析工具。用户双击启动后，在本地 Web 页面上传 MongoDB 日志或最多 20 个 FTDC 文件；日志程序流式解析并生成精确聚合结果，FTDC 程序保留原始压缩文件、建立二进制索引并按单个指标串行查询。

## 技术约束

- 后端使用 Java 17、Spring Boot 和 Maven。
- MongoDB Extended JSON 只使用 MongoDB BSON 库解析，不依赖 MongoDB 服务。
- 前端使用 Vue 3、Vite、Element Plus 和 ECharts，构建产物打包进 Spring Boot JAR。
- 运行时不得依赖 MongoDB、Node.js、Nacos、S3 或其他外部服务。
- 应用默认只监听 `127.0.0.1`。
- 持久化采用本地 JSON 和 JSONL 文件，不引入数据库。

## 目录约定

```text
mongodb-log/
├── CLAUDE.md
├── ROADMAP.md
├── README.md
├── pom.xml
├── docs/
│   └── superpowers/
│       ├── specs/
│       └── plans/
├── src/
│   ├── main/
│   │   ├── java/com/vaultsphere/mongodblog/
│   │   └── resources/
│   └── test/
│       ├── java/com/vaultsphere/mongodblog/
│       └── resources/fixtures/
├── web/
│   ├── src/
│   └── tests/
└── scripts/
```

后端按 `parser`、`analysis`、`task`、`storage`、`web` 分包，FTDC 子系统在对应包下使用 `ftdc` 子包并与日志任务隔离。一个类只承担一个明确职责，不把数据模型、解析、统计和接口混在同一个类中。

业务元数据继续使用本地 JSON／JSONL。FTDC 任务允许在 `data/ftdc` 下保存上传的原始压缩文件和应用生成的 `blocks.idx` 二进制索引；不得把全量展开后的时间序列保存为 JSON、JSONL、数据库行或其他重复副本。

## 解析与统计红线

- 所有日志必须流式处理，禁止一次性读入整个上传文件。
- 支持纯文本日志、结构化 JSON 日志以及 `.gz` 压缩日志。
- 每次上传创建一个任务；一个任务可包含多个日志文件。
- Top 5000 必须基于全部慢查询，通过固定容量最小堆精确选取。
- 耗时区间分布必须基于全部慢查询，不能基于 Top 5000 抽样。
- 固定耗时区间为：`<100ms`、`100ms-500ms`、`500ms-1s`、`1s-3s`、`3s-10s`、`10s-30s`、`30s-60s`、`>=60s`。
- 普通日志不保存原文。慢查询除全局 Top 5000 外，允许独立保留 Top 50 查询模式各自最慢的一条原文；分析过程中每个模式最多维护一条最慢样本，最终仅持久化 Top 50 模式样本。
- 解析异常必须计数并分类，不得静默吞掉。
- CPU 统计仅在日志实际包含 `cpuNanos` 时展示。
- 时间统一存储为 UTC Epoch Milliseconds，页面按本机时区展示。
- 新增运行诊断不得改变现有 `summary.json`、Top 5000、Top 50、耗时分布和旧任务接口口径；诊断结果独立保存为 `diagnostics.json`，历史任务缺失时必须兼容读取。
- MongoDB 4.4 及以上结构化日志按字段能力解析，不得仅依赖服务器版本号；未知字段忽略并计入覆盖率，缺失字段显示“日志未提供”，不得按零值处理。
- 运行诊断只保存聚合值、时间桶和每类最多 3 条脱敏样本；不得新增普通日志原文持久化。
- 连接、事件和客户端统计必须流式且有界；时间趋势最多 2,000 点，高基数字段不得用无界 Map 保存全部值。
- Markdown 报告只导出已持久化的聚合结果、规范化查询模式和脱敏诊断信息；不得导出完整命令、原始日志、用户名、密码、token 或完整客户端 IP。

## FTDC 解析与查询红线

- FTDC 外层 BSON 必须逐文档读取，一次只解压当前 block；禁止读取完整文件或缓存全部解压 block。
- 一个 FTDC 任务最多包含 20 个非空文件，文件按内容验证，不依赖扩展名。
- FTDC 上传解析、索引重建、指标查询、原始分页和流式导出共用一个公平的串行许可。
- 查询一次只恢复一个指标；禁止构造全量 `Map<String, List<Long>>` 或用 `List<Long>` 保存完整指标序列。
- 图表结果最多 2,000 点；完整原始值只能按最多 1,000 点分页查看或流式导出 `CSV.gz`。
- `blocks.idx`、catalog 和任务元数据必须通过同目录临时文件加原子移动发布；不完整索引不得进入可查询状态。
- 查询前必须核对索引记录的源文件大小和 SHA-256；不匹配时拒绝查询。
- 发布启动参数统一为 `-Xms128m -Xmx2g`；正确性与内存边界不得依赖 `System.gc()`。

## 开发纪律

- 功能与修复遵循测试驱动开发：先写失败测试，再写最小实现。
- OPS 和 OPS_web 仅作为逻辑与交互参考，不机械复制其错误处理和旧依赖。
- 解析器兼容性测试要逐字段对比 OPS 的 `MongoLogV2`；确认过的旧逻辑错误以独立正确性测试为准。
- 每个统计结果都必须有手工可复算的小数据集测试。
- 前端统计图提供“图表／数据表”切换或可核对数值，同一区块不重复展示两种视图；散点点击在右侧抽屉展示完整日志与基于实际字段的解读。
- 不添加与 MongoDB 日志解析无关的功能。
- 每次完成并验证开发事项后同步更新 `ROADMAP.md`。

## 验证要求

- `mvn test` 必须通过。
- `npm test` 和 `npm run build` 必须通过。
- `mvn package` 必须生成可启动 JAR，并包含前端静态资源。
- 使用真实或等价样例验证纯文本、JSON、GZip、混合格式、异常行和 Top 5000 边界。
- 启动后必须通过浏览器完成上传、进度、结果页和明细查看的端到端验证。
